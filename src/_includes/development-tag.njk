{%- if site.environment == "dev" %}
  <details id="pageDevTag">
    <summary>
      <span>Development</span>
    </summary>
    <div>
      <code>cssComponents</code>
      <textarea>{{ cssComponents | dump }}</textarea>
      <code>page</code>
      <textarea>{{ page | dump }}</textarea>
      <code>site</code>
      <textarea>{{ site | dump }}</textarea>
      <code>eleventy</code>
      <textarea>{{ eleventy | dump }}</textarea>
    </div>
  </details>

  <style>
    #pageDevTag {
      position: sticky;
      width: 100%;
      top: 0;
      left: 0;
      z-index: 9999;
      color: white;
      background-color: #c00;
    }
    #pageDevTag summary { list-style: none; }
    #pageDevTag summary::-webkit-details-marker { display: none; }
    #pageDevTag summary {
      padding: .25em 1em .25em .5em;
      font-size: smaller;
      font-weight: bold;
      text-transform: uppercase;
      cursor: pointer;
    }
    #pageDevTag div {
      display: grid;
      grid-template-columns: 8em 1fr;
      gap: .5em;
      padding: .5em;
    }
    #pageDevTag textarea {
      width: 100%;
      height: 10em;
      resize: vertical;
    }
  </style>
{%- endif %}