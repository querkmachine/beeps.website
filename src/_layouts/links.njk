{% extends 'src/_layouts/generic.njk' %}

{% css %}
.pageButtonGrid {
  display: grid;
  gap: 10px;
  grid-template-columns: repeat(auto-fill, 88px);
  margin-block-end: var(--space-5);
  line-height: 0;
}
.pageButtonGrid a {
  display: block;
}
.pageButtonGrid img {
  margin-block-end: 0;
}
{% endcss %}
