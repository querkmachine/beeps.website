{% extends 'src/_layouts/generic.njk' %}

{% css %}
.pageSwatch {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
}

.pageSwatch_preview {
  width: 1.75rem;
  height: 1.75rem;
  border: 1px solid currentcolor;
  forced-color-adjust: none;
}

.pageLogo {
  max-width: 14em;
}

.pageIconGrid {
  display: flex;
  flex-wrap: wrap; 
  gap: var(--space-3);
}
{% endcss %}