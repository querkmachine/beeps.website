---
layout: layouts/base.njk
eleventyNavigation:
  key: Home
  order: 1
title: Kimberly Grey — Bristol-based web developer
foot: |
  <script>
    // Funky letter swapping fun
    (() => {
      const $logo = document.querySelector(".kimMasthead_switcheroo");
      const children = $logo.children;
      const effects = ['text', 'flipX', 'flipY', 'flipZ'];
      const getRandom = (arr) => {
        return arr[Math.floor(Math.random() * arr.length)];
      }
      const addRandomEffect = () => {
        const $child = getRandom(children);
        $child.classList.add(getRandom(effects));
      };
      const removeRandomEffect = () => {
        const $child = getRandom(children);
        $child.classList.remove(getRandom($child.classList));
      }
      const changeSomething = () => {
        const functions = [addRandomEffect, removeRandomEffect];
        getRandom(functions)();
      }
      setInterval(changeSomething, 100);
    })();
  </script>
---

<hr class="kimRule kimRule--s kimRule--visible">
<div class="kim-!-padding-top-9 kim-!-padding-bottom-9 kim-!-background-color-black kim-!-text-color-white">
  <div class="kimWrapper">
    <h1 class="kimHeading-xl">Hi, I&rsquo;m <br>Kimberly&nbsp;Grey.</h1>
    <div class="kimGrid">
      <div class="kimGrid_a2-a4">
        <p class="kimBody">I&rsquo;m a Bristol-based front-end developer specialising in design systems, accessibility, and the semantic web. I currently work as lead front-end developer &amp; accessibility and inclusivity advocate at <a href="//felinesoft.com">Felinesoft</a>.</p>
      </div>
      <div class="kimGrid_a4-a6">
        <p class="kimBody">I'm trans, ace, and in my spare time I'm <a href="{{ '/me' | url }}">a robot bat</a> on the internet.</p>
        <p class="kimBody">This is my personal website where I <a href="{{ '/projects' | url }}">keep random junk</a> and <a href="{{ '/blog' | url }}">blog occassional nonsense</a>.</p>
      </div>
    </div>
  </div>
</div>
<hr class="kimRule kimRule--s kimRule--visible">